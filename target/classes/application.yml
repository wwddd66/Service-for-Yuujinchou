# 配置文件的真实位置
spring:
  messages:
    basename: i18n.login

---
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/urz_db?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

# 整合mybatis
mybatis:
  type-aliases-package: com.dane.springboot.demo.pojo
  mapper-locations:
    - classpath:mybatis/mapper/*.xml

